#![allow(dead_code, unused)]

use crate::map_gen::{abstract_map::TileMap, abstract_tiles::AbstractMapTiles};
use glam::UVec2;
use std::ops::Range;

pub fn build_bsp_dungeon(tilemap: &mut TileMap, dimensions: &UVec2) {
    todo!();
}
